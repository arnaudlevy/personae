<%= simple_form_for person do |f| %>
  <%= f.association :study, as: :hidden %>
  <%= f.input :code %>
  <h2>Evaluations</h2>
  <table class="table">
    <%= f.simple_fields_for :evaluations do |e| %>
      <tr>
        <td width="300"><%= e.object.variable %></td>
        <td>
          <div class="value_and_no_value">
            <%= e.input :no_value, as: :boolean %>
            <%= e.input :value,
                        as: :range,
                        label: false,
                        html: {
                          style: 'width: 100%'
                        } %>
          </div>
        </td>
      </tr>
    <% end %>
  </table>
  <%= f.submit 'Enregistrer', class: 'btn btn-primary btn-sm' %>
<% end %>
